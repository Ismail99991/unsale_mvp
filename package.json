{
  "name": "unsale-mvp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm run install:web && cd apps/web && npm run dev",
    "dev:admin": "npm run install:admin && cd apps/admin && npm run dev",
    "build:web": "npm run install:web && cd apps/web && npm run build",
    "build:admin": "npm run install:admin && cd apps/admin && npm run build",
    "install:web": "cd apps/web && npm install",
    "install:admin": "cd apps/admin && npm install",
    "install:all": "npm install && npm run install:web && npm run install:admin"
  },
  "engines": {
    "node": ">=18.x"
  }
}
